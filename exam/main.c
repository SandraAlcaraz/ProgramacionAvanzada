#include <stdio.h>
#include <string.h>
#include <stdlib.h>
void reordenaPalabra(String s){
  char []=s;
  int *p=s;
  Str dummy;
  for (int i=0; i<10);i++){
    if(char[i]==" "){
    printf("%s\n",  strcat(s[], ));//copiar desde s[i+i] y de
break;
    }
  }
}

int int main() {
  int input;
  scanf("%s\n",input );
for(int i=0; i<input++){
  char *a = (char*) malloc (sizeof (char));
  scanf ("%c", a);
}

  return 0;
}
